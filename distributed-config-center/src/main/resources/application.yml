spring:
  application:
    name: distributed-config-center

  cloud:
    config:
      server:
        git:
          uri: https://github.com/g0rden/config-center-files.git
          searchPaths: distributed-config-center/config-repo
          username: black.mesa.gorden.freeman@gmail.com
          password: p@ssw0rd3465

server:
  port: 9876
